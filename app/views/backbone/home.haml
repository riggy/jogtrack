= content_for :backbone_init do
  - if current_user
    - javascript_tag do
      :plain
        var options = {
          user: {
            email: "#{current_user.email}",
            id: #{current_user.id}
          }
        }



#home